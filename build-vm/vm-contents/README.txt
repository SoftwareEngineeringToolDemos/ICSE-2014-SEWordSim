The tool will automatically be launched after the startup.

To access the tool later on, 
1) Double click on launch_SE_WordSim.sh which is present on the desktop
2) This leads to launching the tool and in order to get the similarity, input two words to be compared.
   eg. folder and directory
3) Enter. This will printout the results.

Default Password : Test@123
